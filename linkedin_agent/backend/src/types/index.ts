export interface Request extends Express.Request {
    // Define any custom properties for the request object here
}

export interface Response extends Express.Response {
    // Define any custom properties for the response object here
}